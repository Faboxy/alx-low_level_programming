#include "main.h"

/**
* _strlen - Entry point
* Description: prints the lenght of a string
* @s - lenght of characters to be printed
* Return: i
*/

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{
		;
	}
	return (i);
}
