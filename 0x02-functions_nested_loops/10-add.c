#include "main.h"
#include <stdio.h>

/**
 * add - prints and two number and returns the result
 * @i: print int i
 * @k: print int k
 * Return: always 0
 */

int add(int i, int k)
{
	return (i + k);
}
