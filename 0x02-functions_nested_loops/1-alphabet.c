#include "main.h"
/**
 * mian - Entry point
 * A program that writes all the alphabets in lowercase a-z
 * Return: 0
 */
void print_alphabet(void);
}
	char x;
	for(x = 'a'; x <= 'z'; x++)
		_putchar('n');
		_putchar('\n');
		return (0);
}
